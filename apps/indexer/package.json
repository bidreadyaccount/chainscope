{
  "name": "@chainscope/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "viem-based chain indexer + DEX adapter framework (Phase 4).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@chainscope/config": "workspace:*",
    "@chainscope/database": "workspace:*",
    "@chainscope/shared": "workspace:*"
  }
}
